# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Secreto usado para firmar (HMAC-SHA256) el token de sesión del panel admin.
# Genera uno único y aleatorio, por ejemplo con: openssl rand -hex 32
# NO reutilices este valor entre entornos (dev/prod) ni lo compartas.
SESSION_SECRET=
